# 安全策略

## 支持的版本

| 版本                 | 状态   |
| -------------------- | ------ |
| 最新版本（含预发布） | 支持   |
| 更早版本             | 不保证 |

## 报告漏洞

发现安全漏洞时，请勿在公开 Issue 中披露。请通过 GitHub 仓库 Security 页的「Report a vulnerability」私密提交，或联系维护者。确认后会在修复版本中处理。

## 安全模型

- **serve 鉴权**：默认监听回环地址（`127.0.0.1`）免令牌；绑定非回环地址时强制令牌（`X-BBDown-Token` 请求头或 `?token=` 查询参数），否则返回 401。
- **SSRF 防护**：任务回调地址仅允许公网 http/https，拒绝内网与回环地址，并在建立连接前对最终端点 IP 二次校验。
- **CORS**：默认放行回环来源，非回环 `Origin` 仍被浏览器拦截；显式传 `--cors-origin` 时叠加对该单一来源开放。
- **凭据**：统一存于本地 `BBDown.data`，类 Unix 系统落盘权限收紧为 600。
- **外部后处理**：主程序不解析任何加密特征，密钥与加密信息由外部进程自行管理。

设计细节见 [ARCHITECTURE.md](./ARCHITECTURE.md)。
