<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <OutputType>library</OutputType>
        <!-- 主项目的 AOT 配置在 BBDown/Directory.Build.props 里，不覆盖 Core；单独开启才能在编译 Core 时就跑 AOT/Trim 分析器 -->
        <IsAotCompatible>true</IsAotCompatible>
    </PropertyGroup>
    <ItemGroup>
        <PackageReference Include="Downloader" />
        <PackageReference Include="Google.Protobuf" />
        <PackageReference Include="Grpc.Tools">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <!-- 登录二维码生成（QRCoder 随 CLI AOT 构建已验证兼容） -->
        <PackageReference Include="QRCoder" />
    </ItemGroup>
    <ItemGroup>
        <!-- 这些 proto 只有 message 没有 service，生成 gRPC 客户端桩会产出一批 0 字节文件 -->
        <Protobuf Include="APP\**\*.proto" GrpcServices="None" />
    </ItemGroup>
    <ItemGroup>
        <InternalsVisibleTo Include="BBDown.Core.Tests" />
        <!-- 主项目的下载层测试要替换 HTTPUtil.AppHttpClient 来 stub Range 响应 -->
        <InternalsVisibleTo Include="BBDown.Tests" />
    </ItemGroup>
</Project>
